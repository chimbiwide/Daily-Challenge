# C — Pointer Reversal Puzzle

Reverse an int array in place using only pointer arithmetic (no [] indexing), two-pointer swap technique, O(1) extra space, no library helpers. Bonus: print the result with a pointer-only loop. Reinforces pointer/array duality in C.
